import createShareableLink from 'dummy/utils/create-shareable-link';
import { module, test } from 'qunit';

module('Unit | Utility | create-shareable-link', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = createShareableLink();
        assert.ok(result);
    });
});
