import getGatewaySchemas from 'dummy/utils/get-gateway-schemas';
import { module, test } from 'qunit';

module('Unit | Utility | get-gateway-schemas', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = getGatewaySchemas();
        assert.ok(result);
    });
});
