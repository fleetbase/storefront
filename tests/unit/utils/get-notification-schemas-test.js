import getNotificationSchemas from 'dummy/utils/get-notification-schemas';
import { module, test } from 'qunit';

module('Unit | Utility | get-notification-schemas', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = getNotificationSchemas();
        assert.ok(result);
    });
});
